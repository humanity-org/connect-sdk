# Use default npm registry for @humanity-org scope
@humanity-org:registry=https://registry.npmjs.org

# Auth token - set NPM_TOKEN env var or replace with your token
//registry.npmjs.org/:_authToken=${NPM_TOKEN}

