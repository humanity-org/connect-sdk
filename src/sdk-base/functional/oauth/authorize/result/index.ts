/**
 * @packageDocumentation
 * @module api.functional.oauth.authorize.result
 * @nestia Generated by Nestia - https://github.com/samchon/nestia
 */
//================================================================
import type { IConnection } from "@nestia/fetcher";
import { PlainFetcher } from "@nestia/fetcher/lib/PlainFetcher";

import type { AuthorizeResultQuery } from "../../../../structures/AuthorizeResultQuery";

/**
 * @controller OauthController.authorizeResult
 * @path GET /oauth/authorize/result
 * @accessor api.functional.oauth.authorize.result.authorizeResult
 * @nestia Generated by Nestia - https://github.com/samchon/nestia
 */
export async function authorizeResult(
  connection: IConnection,
  query: authorizeResult.Query,
): Promise<void> {
  return PlainFetcher.fetch(connection, {
    ...authorizeResult.METADATA,
    template: authorizeResult.METADATA.path,
    path: authorizeResult.path(query),
  });
}
export namespace authorizeResult {
  export type Query = AuthorizeResultQuery;

  export const METADATA = {
    method: "GET",
    path: "/oauth/authorize/result",
    request: null,
    response: {
      type: "application/json",
      encrypted: false,
    },
    status: 200,
  } as const;

  export const path = (query: Query) => {
    const variables: URLSearchParams = new URLSearchParams();
    for (const [key, value] of Object.entries(query as any))
      if (undefined === value) continue;
      else if (Array.isArray(value))
        value.forEach((elem: any) => variables.append(key, String(elem)));
      else variables.set(key, String(value));
    const location: string = "/oauth/authorize/result";
    return 0 === variables.size
      ? location
      : `${location}?${variables.toString()}`;
  };
}
